int main()
{
	char	*login = NULL;
	char	*pass = NULL;
	char	line[1024];

	while(42)
	{
		printf(pass, login);
		if (fgets(&line, 1024, stdin) == NULL)
			break ;
		if (ft_strncmp(line, "auth ", 5))
		{
			
		}
	}
}
