#include <stdio.h>

int	m;

int	p(char	*buff)
{
	return(printf(buff));
}

void	n()
{
	char	buff[512]

	fgets(&buff, 512, stdin);
	m = p(&buff);
	if (m == 0x1025544)
	{
		system("/bin/cat /home/user/level5/.pass");
	}
}

int	main()
{
	n();
}
